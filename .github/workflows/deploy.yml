name: Deploy to Vercel

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SOCKET_URL: https://hpf-socket-server.onrender.com
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run Type Check
        run: npx tsc --noEmit

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel environment variables
        continue-on-error: true
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Set NEXT_PUBLIC_SOCKET_URL in Vercel project
        continue-on-error: true
        run: |
          echo "${{ env.NEXT_PUBLIC_SOCKET_URL }}" | vercel env add NEXT_PUBLIC_SOCKET_URL production --token=${{ secrets.VERCEL_TOKEN }} --force || true

      - name: Ensure production socket URL
        run: |
          touch .env.production.local
          grep -v "^NEXT_PUBLIC_SOCKET_URL=" .env.production.local > .env.production.local.tmp || true
          mv .env.production.local.tmp .env.production.local
          echo "NEXT_PUBLIC_SOCKET_URL=${NEXT_PUBLIC_SOCKET_URL}" >> .env.production.local

      - name: Deploy to Vercel (source deploy — Vercel cloud builds)
        run: vercel --prod --yes --token=${{ secrets.VERCEL_TOKEN }}
